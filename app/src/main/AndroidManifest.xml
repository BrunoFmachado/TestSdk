<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name="com.example.SmartPosSdkApp"
        android:allowBackup="true"
        android:icon="@android:drawable/checkbox_on_background"
        android:label="@string/app_name"
        android:roundIcon="@drawable/splash_background"
        android:supportsRtl="true"
        android:theme="@style/Theme.SmartPosSdk">
        <activity
            android:name="com.example.presentation.activity.SplashArt"
            android:exported="true"
            android:theme="@style/Theme.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.example.presentation.activity.MainActivity"
            android:exported="true" />
    </application>

</manifest>
